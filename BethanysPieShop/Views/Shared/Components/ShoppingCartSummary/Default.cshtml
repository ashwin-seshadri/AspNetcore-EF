@model ShoppingCartViewModel

@if(Model.ShoppingCart.Items.Count > 0)
{
    <li>
        <a>
            <span class="glyphicon glyphicon-shopping-cart"></span>
            <span id="cart-status">@Model.ShoppingCart.Items.Count</span>
        </a>
    </li>
}